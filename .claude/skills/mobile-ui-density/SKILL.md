---
name: mobile-ui-density
description: Use whenever creating or changing anything the user sees in the BodyBatteries app (screens, cards, lists, sheets, forms). Compact phone UI rules the owner approved — fixed-height scroll windows, fold level by level (Month › Week › Day), secondary text behind ⓘ/✎/＋, abbreviated repeated labels with horizontal swipe, balanced symmetric rows — with the existing components to reuse.
---

Read and follow `.ai/skills/mobile-ui-density.md` (the project's source of truth for this skill), including its
checklist before reporting a UI change as done. Keep the i18n rule from `AGENTS.md` (vi/en/de, no hardcoded strings).
When the owner approves or refines a new display pattern, add it to that file (session-wrapup step 5b).
